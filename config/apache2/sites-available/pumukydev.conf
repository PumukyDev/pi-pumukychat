<VirtualHost *:80>
    ServerAdmin webmaster@pumukydev.com
    ServerName pumukydev.com
    DocumentRoot /var/www/htdocs
    DirectoryIndex index.php
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    <Files "logo.png">
        ForceType application/octet-stream
        Header set Content-Disposition "attachment; filename=logo.png"
    </Files>

    <Directory "/var/www/htdocs/admin">
        AuthType Basic
        AuthName "Restricted Content"
        AuthUserFile /etc/apache2/.htpasswd
        Require valid-user
    </Directory>

    ErrorDocument 404 /errors/error404.php

  #  <Location "/status">
  #      AuthType Basic 
  #      AuthName "sysadmin"
  #      AuthUserFile /etc/apache2/.htpasswd
  #      SetHandler server-status
  #      Require user sysadmin
  #  </Location>

    Redirect permanent /status http://grafana.pumukydev.com/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@pumukydev.com
    ServerName grafana.pumukydev.com

    ProxyPreserveHost On
    ProxyPass / http://localhost:3000/
    ProxyPassReverse / http://localhost:3000/

    # Redirigir tráfico a HTTPS
    Redirect permanent / https://grafana.pumukydev.com/
</VirtualHost>